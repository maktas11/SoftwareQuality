# SoftwareQuality

## Run

1. Install dependency:
	- `pip install cryptography`
	- or `pip install -r requirements.txt`
2. Start the console app:
	- `python src/um_members.py`

Super Administrator credentials (hard-coded for assessment):
- Username: super_admin
- Password: Admin_123?

The app stores encrypted data, logs, and backups under `./data/`.